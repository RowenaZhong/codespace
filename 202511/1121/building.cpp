#include <bits/stdc++.h>
using namespace std;
int n, q;
int main()
{
    freopen("building.in", "r", stdin);
    freopen("building.out", "w", stdout);
    ios::sync_with_stdio(false);
    cin.tie(0);
    cin >> n >> q;
    for (int i = 1; i <= q; i++)
        cout << 0 << endl;
    return 0;
}